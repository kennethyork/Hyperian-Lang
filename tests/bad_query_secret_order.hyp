application "Unsafe Query Order" is service

model Account
    field email is text required
    field password is secret required
end

controller Accounts
    when application starts
        collect every Account email ordered by password as emails
        show view "main"
    end
end

view "main"
    heading "Unsafe"
end
