application "Unsafe Query Filter" is service

model Account
    field email is text required
    field password is secret required
end

controller Accounts
    when application starts
        collect every Account email where password is attempt as emails
        show view "main"
    end
end

view "main"
    heading "Unsafe"
end
