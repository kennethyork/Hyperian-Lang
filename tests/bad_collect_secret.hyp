application "Unsafe Collection" is service

model Account
    field password is secret required
end

controller Accounts
    action load
        collect every Account password as passwords
    end

    when application starts
        run action load
        show view "result"
    end
end

view "result"
    show passwords
end
