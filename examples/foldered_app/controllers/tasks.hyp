controller Tasks
    when someone visits "/"
        find all Task as tasks
        show view "task-list" with tasks
    end

    when someone submits "/tasks"
        create Task from form
        redirect to "/"
    end
end
